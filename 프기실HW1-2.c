#include <stdio.h>
int main(void)
{
	int PN;
	float RP;
	float sum;
	scanf("%d %f", &PN, &RP); 
	sum = PN * RP;
	printf("The total retail value was: %.2f", sum);
	switch (PN) {
		case -1 :
				break;
	}

		
		return 0;
}